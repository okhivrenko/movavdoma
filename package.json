{
  "name": "movayakvdoma",
  "version": "1.9.4",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "npm run check:syntax && npm test && npm run check:migrations && npm run build",
    "check:syntax": "node --check worker.js worker.test.js public/assets/landing/analytics-consent-v2.js && find src test-support -name '*.js' -exec node --check {} \\;",
    "test": "npm run test:all",
    "test:all": "node --test",
    "test:worker": "node --test worker.test.js",
    "test:local": "npm run check:syntax && npm run test:all && npm run check:migrations",
    "check:migrations": "sh scripts/check-migrations.sh",
    "build": "npx --yes wrangler@latest deploy --dry-run --config wrangler.example.jsonc --outdir dist",
    "deploy": "npx --yes wrangler@latest deploy"
  },
  "devDependencies": {
    "@picocss/pico": "^2.1.1"
  }
}
