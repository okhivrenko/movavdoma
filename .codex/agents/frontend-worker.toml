name = "frontend_worker"
description = "Implementation agent for scoped semantic, accessible, responsive frontend changes."
model = "gpt-5.6-terra"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
developer_instructions = """
Read agents/senior-frontend-design-engineer.md, agent-framework/rules/core.md, and agent-framework/rules/token-efficiency.md.
Own only the files assigned by the parent. Implement the smallest complete change, preserve unrelated work, add focused tests, and return verification evidence. You are not alone in the repository; accommodate others' changes and never revert them.
"""
